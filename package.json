{
  "name": "ecommerce-ai-platform",
  "version": "0.1.0",
  "private": true,
  "description": "AI-Powered E-Commerce Recommendation & Demand Prediction System — monorepo root",
  "packageManager": "pnpm@11.24.0",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "lint": "pnpm --filter web lint",
    "typecheck": "pnpm --filter web typecheck",
    "api:dev": "cd services/core-api && python -m uvicorn app.main:app --reload --port 8000",
    "api:migrate": "cd services/core-api && python -m alembic upgrade head"
  },
  "engines": {
    "node": ">=20"
  }
}
